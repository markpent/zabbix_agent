:plugins:
  :ha_proxy:
    :disabled: :retry
    :interval: 5
    :backends: true
    :frontends: false
    :servers: false
    :attributes: [qcur, qmax, scur, smax, bin, bout, econ, eresp]
    :socket_file: /var/run/haproxy.sock
  :passenger:
    :disabled: :retry
    :interval: 5
:reporting_rate: 300

  